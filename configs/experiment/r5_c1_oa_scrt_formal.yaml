# 验证状态：R5-C1-OA-SCRT 正式 12-epoch 筛选配置（2026-08-31 r2 执行，inconclusive，SAR 零假设全过）
# 分类：通过验证的当前有效配置（两区制 2026-09-02）。
pretrained_initialization: dinov2_b14_plus_croma_base_sar
data_split: train_validation
preprocessing: aligned_shared_grid_v1
augmentation: non_cloud_v1
sampler: group_disjoint_v1
micro_batch: 4
effective_batch: 8
optimizer: adamw
learning_rate: 0.0001
scheduler: cosine
evaluator: segmentation_v1
nms: not_applicable_segmentation
seed: 0
epochs: 12
execution_epochs: 12
scheduler_horizon_epochs: 12
rapid_screen_horizon_epochs: 6
common_init_seed: 0
innovation_init_seed: 1000
data_order_seed: 0
augmentation_seed: 0
gradient_accumulation_steps: 2
weight_decay: 0.01
num_workers: 4
prefetch_factor: 2
persistent_workers: true
unimodal_mode: multimodal
loss_graph_signature: segmentation
loss:
  segmentation_weight: 1.0
  honesty_weight: 0.0
  counterfactual_weight: 0.0
  auxiliary_sar_weight: 0.0
  degradation_sampler_version: none
enabled_mechanism_ids: [R5-C1-OA-SCRT]
external_trainable_component: false
backbone_mode: official_cloud_frozen
